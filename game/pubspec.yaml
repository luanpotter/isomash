name: isomash
description: An isometric experiment (so far).
publish_to: none
version: 0.2.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  dartlin: ^0.6.3
  flame: ^1.8.0
  flutter:
    sdk: flutter

dev_dependencies:
  flame_lint: ^1.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: false

  assets:
    - assets/images/
